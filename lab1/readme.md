# EventLoop

js is synchronous and single threaded bydefault

## there can be async behaviour

-with BrowserAPT-
setTimeout, setInterval, setImmediate, nextTick
-with promises
-with event handlers3  

morden javca script didivide i two categrogy 
1st commmonjs (.cj)-> supports oops -> require
-priority (nextick, promise ,setimmediate/set timeout)
2nd modulejs (.mjs) -> follow modular approach ->import
-priority(promise,nesttick,setimmediate/settimeout)
